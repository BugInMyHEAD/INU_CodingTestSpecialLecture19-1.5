{
  "tasks": [
    {
      "type": "shell",
      "label": "g++.exe build active file",
      "command": "g++",
      "args": [
        "-g",
        "${file}",
        "-std=c++17",
        "-o",
        //"${fileDirname}\\${fileBasenameNoExtension}.exe"
        "${workspaceFolder}\\out\\build\\test\\test.exe"
      ],
      "options": {
      }
    }
  ],
  "version": "2.0.0"
}
